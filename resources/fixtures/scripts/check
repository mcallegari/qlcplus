#!/bin/sh
DIR=`dirname $0`

xmllint --noout --schema "$DIR"/../../schemas/fixture.xsd $(find .. -name *.qxf) 2>&1 | grep -v " validates$"
xmllint --noout --schema "$DIR"/../../schemas/fixturesmap.xsd "$DIR"/../FixturesMap.xml 2>&1 | grep -v " validates$"

